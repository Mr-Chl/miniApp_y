//index.js
import { parseTime, wxTools } from '../../utils/util.js'  // 工具箱
import { BASE_URL } from '../../config/config.js' // 接口域名 
Page({
    data:{
    },
    onLoad: function (options,e) {
       
    },
})
